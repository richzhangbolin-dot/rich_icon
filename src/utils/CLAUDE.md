# CLAUDE.md — L2 utils 模块

## 职责

纯函数工具库。无副作用，无 React 依赖。

## 成员清单

| 文件      | 职责                                              |
|-----------|---------------------------------------------------|
| `id.js`   | `genId()` — 生成唯一 ID（时间戳 + 随机数 base36） |
| `svg.js`  | SVG 文件读取、净化、文件名转图标名                |

## 变更协议

- 新增工具函数：在对应文件添加，超过 60 行时拆分为独立文件
