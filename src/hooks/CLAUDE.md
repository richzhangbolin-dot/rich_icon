# CLAUDE.md — L2 hooks 模块

## 职责

业务逻辑 hooks，不含副作用 I/O（I/O 在 store 层）。

## 成员清单

| 文件                  | 职责                                             |
|-----------------------|--------------------------------------------------|
| `useIconFilter.js`    | 按分类 + 关键词过滤图标列表，memoized             |

## 变更协议

- 新增过滤维度：修改 `useIconFilter.js`，更新函数签名文档
